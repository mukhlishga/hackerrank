SELECT DISTINCT(CITY) 
FROM STATION 
WHERE 
    CITY LIKE '%A'
    OR CITY LIKE '%I'
    OR CITY LIKE '%U'
    OR CITY LIKE '%E'
    OR CITY LIKE '%O';

or:

SELECT DISTINCT CITY 
FROM STATION 
WHERE RIGHT(CITY, 1) IN ('a', 'e', 'i', 'o', 'u');